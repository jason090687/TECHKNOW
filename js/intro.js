function navigateTo(option) {
    if (option === 'customer') {
        window.location.href = 'login.php'; // Replace with actual customer page URL
    } else if (option === 'seller') {
        window.location.href = 'seller_login.php'; // Replace with actual seller page URL
    }
}
